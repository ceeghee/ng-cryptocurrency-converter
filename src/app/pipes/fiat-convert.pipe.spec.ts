import { FiatConvertPipe } from './fiat-convert.pipe';

describe('FiatConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new FiatConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
