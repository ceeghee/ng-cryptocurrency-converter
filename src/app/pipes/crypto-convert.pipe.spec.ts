import { CryptoConvertPipe } from './crypto-convert.pipe';

describe('CryptoConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new CryptoConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
